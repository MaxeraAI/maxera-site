export const metadata = {
  title: "How a home services company stopped losing after-hours jobs to the competition",
  description:
    "An illustrative Maxera scenario: how a 60-second AI lead response system, running 24/7, flipped the booked-job rate at a typical home services company.",
};

export default function Layout({ children }) {
  return children;
}
