export const metadata = {
  title: "Stop reading about AI. Start putting it to work.",
  description:
    "Most owners we talk to have read every AI thread on LinkedIn and watched every YouTube tutorial. Here’s the simpler way to think about it.",
};

export default function Layout({ children }) {
  return children;
}
